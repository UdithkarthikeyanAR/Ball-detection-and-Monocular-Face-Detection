# Core — required for geometry, tracking, benchmarking and tests.
numpy>=1.24
opencv-python>=4.6      # SOLVEPNP_SQPNP needs >= 4.4
scipy>=1.10

# Live capture only. The rest of the package imports and tests without it.
mediapipe>=0.10.0

# Web instrument panel (python -m facedist.server).
flask>=3.0

# Development.
pytest>=7.0
